node_modules/
out/
dist/
.vite/
# `pnpm test:coverage` writes here. `reports/` is committed on purpose and this is not: the report is a
# summary worth reviewing in a diff, the coverage directory is a few thousand generated HTML files.
coverage/
.stryker-tmp/
# Agent worktrees and throwaway spikes live inside the checkout but are never part of it.
.claude/worktrees/
scripts/spike-mux/
*.tsbuildinfo
.DS_Store
.env
.env.local
*.log
